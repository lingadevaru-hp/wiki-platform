// wiki-platform — public interface surface. Implementation is private.
// Shared utilities: locale resolution, content loading, SEO helpers.
//
// Stable surface:
//   - resolveLocale(hostname): LangCode
//   - loadArticle(slug, locale): ArticleMeta
//   - hreflangLinks(slug): { lang, href }[]

export {}; // bodies live in the private core
