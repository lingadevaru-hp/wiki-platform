# Scripts

Ops helpers. Usage only — runnable bodies live in the private core.

| Script | Purpose |
|--------|---------|
| `mirror-platform.sh` | Publish per-push release notes + changelog entries here |
| `deploy.sh` | Build + deploy the web app to the edge |
| `locales.sh` | Validate locale dictionaries across all languages |
